# ⚠️ Legacy / Unused

This document describes an early prototype API (username/password login, `/api/student/jobs`, `/api/admin/*`, mock CV upload endpoints) that does not match the current, deployed API. It is not served by `vercel.json`.

For accurate, current API documentation, see the root [`README.md`](../README.md#api-reference) — endpoints are Campus One SSO-authenticated (cookie-based, no bearer tokens), live under `api/*/index.go` at the repo root, and are routed via `vercel.json` rewrites.

This file is kept only for historical reference.
